import { FilterloaiphimPipe } from './filterloaiphim.pipe';

describe('FilterloaiphimPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterloaiphimPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterPipe03Pipe } from './filter-pipe03.pipe';

describe('FilterPipe03Pipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipe03Pipe();
    expect(pipe).toBeTruthy();
  });
});

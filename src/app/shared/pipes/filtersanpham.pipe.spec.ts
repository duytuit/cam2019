import { FiltersanphamPipe } from './filtersanpham.pipe';

describe('FiltersanphamPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersanphamPipe();
    expect(pipe).toBeTruthy();
  });
});
